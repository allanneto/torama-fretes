export default interface ICreateIntentionDTO {
  zip_code_start: string;
  zip_code_end: string;
  quantity: number;
  height: number;
  length: number;
  width: number;
  price: number;
  weight: number;
}
